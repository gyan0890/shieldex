# ── Stellar Network ───────────────────────────────────────────
# Soroban RPC endpoint (testnet)
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# ── Agent Wallet ──────────────────────────────────────────────
# The agent's signing keypair (secret key — keep this private!)
AGENT_SECRET_KEY=S...

# ── Contract (from Person A) ──────────────────────────────────
# TODO: Replace with the deployed Agent Policy Wallet contract address
CONTRACT_ADDRESS=PLACEHOLDER_CONTRACT_ADDRESS

# ── Server ────────────────────────────────────────────────────
PORT=3000
